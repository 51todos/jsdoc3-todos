/** 命名空间: Documents
 * @namespace
 */
var Documents = {
	/**
	 * Solves equations of the form a * x = b
	 * @example
	 * // returns 2
	 * globalNS.method1(5, 10);
	 * @example
	 * // returns 3
	 */
	Newspaper: 1,

	/** @private */
	Diary: 2
};